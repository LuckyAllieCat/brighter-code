# Brighter Code

Genepy-style coding exercises for Python, SQL and R that you type on your phone. Your code really runs in the browser (Pyodide, webR and sql.js) and is checked against tests.

The exercises are generated from the Brighter Path lessons: `node build.js` reads `../quest-path/content/*.js` and writes `index.html`. Progress is saved in the browser.
